# THC Reviews

Generated by the Satellite Site Generator.

## Quick Start

```bash
npm install
npm run dev
```

Then open [http://localhost:5173](http://localhost:5173).

## Build for Production

```bash
npm run build
```

Output goes to `dist/`. Deploy to Vercel, Netlify, or any static host.

## Site Structure

- **Domain:** thcreviews.co
- **Articles:** 25
- **Categories:** 5

### Categories

- **Edibles & Gummies** (`/edibles-gummies`)
- **Tinctures & Oils** (`/tinctures-oils`)
- **Microdose Products** (`/microdose-products`)
- **Sleep & Relaxation** (`/sleep-relaxation`)
- **Energy & Focus** (`/energy-focus`)

## How to Add an Article

1. Open `client/src/data/articles.ts`
2. Add a new object to the `articles` array
3. Required fields: `id`, `slug`, `title`, `category`, `categorySlug`, `metaDescription`, `excerpt`, `thumbnail`, `altText`, `datePublished`, `dateModified`, `content`
4. The homepage and category pages update automatically

## How to Update an Article

1. Find the article by its slug in `client/src/data/articles.ts`
2. Edit the `content` field (supports Markdown)
3. Update `dateModified` to today's date

## SEO

All SEO metadata is stored in `seo-manifest.json`. Edit values there for easy reference.

Each page includes:
- Title and meta description
- Open Graph and Twitter Card tags
- JSON-LD structured data (Article, CollectionPage, BreadcrumbList, WebSite)
- Canonical URLs

## Deployment

### Vercel

```bash
npx vercel
```

### Netlify

Drag and drop the `dist/` folder to [app.netlify.com](https://app.netlify.com).

### Manual

Run `npm run build` and serve the `dist/` folder with any static file server.

---

Generated on 2026-04-25
